<%- include('./partials/head.ejs')  %>
<%- include('./partials/nav.ejs')  %>
<% if (blogs.length > 0) { %>
<% blogs.forEach(user => { %>
<div class="container cont5">
  <div class="row">
    <div class="col-sm-6">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= user.title %></h5>
          <p class="card-text"><%= user.snippet %></p>
          <a href="#" class="btn btn-primary"><%= user.body %></a>
          <center><span>
            <%= user.createdAt%> 
            </span></center>
        </div>
      </div>
    </div>
    <% }) %>

    <% } %>